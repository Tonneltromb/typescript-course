// данная запись говорит о том,
// что переменная myFunc имеет тип "функция" с указанными параметрами и возвращаемым значением типа число
let myFunc: (param1: number, param2: number) => number;

function func(numder1: number, number2: number) { return numder1 + number2; }
myFunc = func;
